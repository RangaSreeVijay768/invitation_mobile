import {CoreConfirmDirective} from './core-confirm.directive';

describe('JeewithConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreConfirmDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
